const name = "Christine";
const age = 25;
const message = `My name is ${name} and I am ${age} years old.`;

console.log(message);

// 反引號（`）支援換行
const message3 = `你好，
我是${name}，
今年${age}歲。`;
console.log(message3);